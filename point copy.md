HLE/MSE n'est pas un MediaStream API. L'idée serait de pouvoir manipuler le flux vidéo comme un objet MediaStream(), mais MSE fonctionne différemment. Un "WebRTC server" serait une autre approche, plus complexe, pour le streaming en temps réel.

HLE/MSE N'est pas un mediastream API? souhaite un sorte d'objet int var = mediastream() pour gérer le flux vidéo? "WebRTC" server?.